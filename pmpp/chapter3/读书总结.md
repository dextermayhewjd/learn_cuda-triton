第三章最重要的就是thread 和 data 的mapping 

`grey_scale.cu`  和 `matmul.cu` 
体现的思想就是 thread和mapping  
thread 是通过kernel函数分配的 所以可能会多 通过if row 和 width 来限制  
因为高纬度矩阵都是通过变化为1d之后读取数据的 所以mapping的步骤  
`row* Width + col` 一般就是生成output的mapping  
但是数据的input读取  如何找到对应的input是这一个章节的课题  

同时要注意的是 cuda所用的都是row major 的存储 



其余知识点可以看 

1. All threads identify their position using built-in index variables:  
 `threadIdx` (within a block) and `blockIdx` (within a grid).  
 `blockDim` and `gridDim` describe the **shape**, not the coordinates.  
	📌 **一句话记忆**
	thread依靠 threadIdx和 blockIdx来确定自己的坐标 
2. a grid is a three-dimensional (3D) array of blocks and each block is a 3D array of threads
	   - 最多是 3D”
	- 实际上可以是 1D / 2D / 3D
3. calling a kernel using execution configuration parameters(<<<. . .>>>)
		启动一个核函数 执行配置参数
	1. first argument : dimension of the grid in number of blocks(一个grid 几个block)  
	   gridDim 是blocks 的组织方式
	   网格的维度 定义有多少个块
	   
	2. second argument : dimension of the block dimension（block里的thread）  
	   blockDim是threads 的组织方式
	   是块的维度定义有多少个线程
	   
	[[2.2 cuda c program structure (kernel grid thread)]]
			eg. dim3 dimGrid(32,1,1);
			  dim3 dimBlock(128,1,1);
			  
	3. variable can have legal C variable name
				dim3 dog(32,1,1);
			    dim3 cat(128,1,1);
				vecAddKernel<<<dog, cat>>>
				- CUDA 不要求特殊变量名
				- <<< >>> 接受的是值，不是名字	
4. grid and block dimensions can also be calculated from other variables
	可以由计算别的变量来得出 例如此处的 ceil
				dim3 dimGrid(ceil(n/256.0),1,1);
			    dim3 dimBlock(256,1,1);
			    vecAddKernel<<<dimGrid, dimBlock>>>
			    
5. shorthand convention for 1d configurations takes advantage of how C++ constructor and default parameters 
   调用1D的网格gird和块block的内核
   除了使用dim3 构造（128，1，1）这种
   也可以直接<<<1,1>>>
   在需要dim3的地方传单值 默认y和z的维度是1
   
6. Limit of thread within a block is 1024.
		块的总大小是1024线程 x* y * z那种！！！
		` 一个 block 最多 **1024 threads**`
		eg. dimBlock(512,1,1) ok
				 (8,16,4) ok
				 (32,32,2) not ok    32 * 32 * 2 >1024
7. A grid and its blocks do not need to have the same dimensionality 
			eg. dim3 dimGrid(2,2,1);
			  dim3 dimBlock(4,2,2);
			  KernelFunction<<<dimGrid, dimBlock>>>
8. Multidimensional example of cuda grid organization
		grid1 :   Block(0,0) Block(0,1)
			 Block(1,0) Block(1,1)
			 
- **dim3 构造函数是 `(x, y, z)`**
- **threadIdx / blockIdx 访问顺序也是 `.x .y .z`**
		